import { expect } from "chai"

describe("test", () => {
    it("should properly launch tests", () => {
        expect(false).to.be.false;
    })

    it("should properly launch tests", () => {
        expect(true).to.be.true;
    })
})